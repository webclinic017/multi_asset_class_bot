from setuptools import setup, find_packages

setup(
    name="multi_asset_bot",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Will read from requirements.txt
    ],
)